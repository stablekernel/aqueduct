language: dart
dart:
  - dev
addons:
  postgresql: "9.6"

services:
  - postgresql

before_install:
  - bash ci/before_install.sh

after_success:
  - bash ci/after_script.sh

jobs:
  include:
    - stage: test
      script: bash ci/script.sh
      env: TEST_DIR="aqueduct" DART_VM_OPTIONS=--preview-dart-2
    - stage: test
      script: bash ci/script.sh
      env: TEST_DIR="aqueduct_test" DART_VM_OPTIONS=--preview-dart-2

stages:
  - test

branches:
  only:
    - master
    - 3.0
